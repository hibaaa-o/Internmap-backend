-- Quick PostgreSQL Database Setup for InternMap Backend
-- Run this in psql or your PostgreSQL client

-- Create the database
CREATE DATABASE internmap;

-- Connect to it:
-- \c internmap

-- Or use the connection string in .env:
-- DATABASE_URL=postgres://postgres:sixbits@localhost:5432/internmap

-- The server will auto-create tables (users, internships, applications) on startup
